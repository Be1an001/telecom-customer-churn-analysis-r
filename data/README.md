# Data Note

This repository currently includes the main CSV files used in the original course project, along with a data dictionary reference:

- `telecom_customer_churn.csv`
- `telecom_zipcode_population.csv`
- `telecom_data_dictionary.csv`

The first two datasets were merged by `Zip.Code` during the analysis.

These files are kept in the repository so the project structure and the main EDA steps are easier to follow.
